---
name: llm-wiki
description: 个人知识库 / Wiki / 维基笔记管理系统。当提到「个人知识库、个人wiki、个人维基、个人笔记、建立wiki、整理知识库、存入wiki、存到维基、提炼要点、做个摘要、提取观点、帮我消化这篇文章」时触发。支持 URL/文本/文件夹三种输入，自动存 raw → 创建摘要 → 提取概念/实体/对比/概览。基于 Karpathy LLM Wiki 模式，AI 负责知识整合与维护，人类负责素材采集与方向探索。三层架构：raw(原始素材) → wiki(AI合成) → schema(行为规范)。
---

# LLM Wiki - 个人知识管理系统

## ⚠️ 边界声明

**本 Skill：知识层面的理解与整理** — 提炼要点、存入 wiki 结构化条目

**触发词：** 存到 wiki、整理到知识库、做个摘要、提炼要点、帮我消化

**❌ 不要走本 Skill：** 抓取/备份公众号文章 → 用 `wechat-article-fetcher`

**核心区分：**
| 需求 | Skill |
|------|-------|
| 要文件（下载备份） | wechat-article-fetcher |
| 要知识（提炼存入） | llm-wiki |

---

## 使用规范

### 文档读取顺序

执行任何 LLM Wiki 操作前，AI **必须**按以下顺序读取规范文档：

1. **首先读取本 SKILL.md**（技能级规范）
2. **然后读取项目 CLAUDE.md**（`~/wiki/CLAUDE.md`，项目级规范，与 wiki/ 同级）
3. **两个文档的规范都要遵守**
   - 如有冲突，以 CLAUDE.md 为准（项目级优先于用户级）
   - 如 CLAUDE.md 未规定的事项，参照 SKILL.md 执行

### 核心理念

这是一个基于 Karpathy LLM Wiki 理念的个人 Obsidian 知识库。LLM Wiki 是一种 AI 增强的知识管理范式：AI 不仅回答问题，更承担"簿记"工作——更新交叉引用、标注矛盾、保持一致性。人类聚焦于素材采集、探索提问和方向把控。

**五大原则：**
1. **原始素材不可变** — 永不修改 raw 文档，只增不减
2. **LLM 拥有 Wiki** — 用户阅读，LLM 负责写入和维护
3. **知识复利** — 每一次摄入和查询都会丰富 Wiki
4. **富维基** — 把有价值的分析沉淀回 Wiki，不要让好答案消失在聊天里
5. **定期除絮** — 保持 Wiki 健康生长的同时维持其健康状态

---

## 三层架构概览

```
raw/     → 原始素材（不可变）
wiki/    → AI 合成的结构化知识条目（摘要/实体/概念/对比/概览/综合）
CLAUDE.md → 项目行为规范（完整步骤在 references/ 目录）
```

---

## 核心操作

### 1. Ingest — 摄入素材

**触发词：** 提供链接/文件/粘贴内容/文件夹

**输入：** URL / 粘贴文本 / 文件路径 / 文件夹路径

**概要步骤：** 检查是否已存在 → 解析 → 存储 raw → 创建摘要 → 提取知识层 → 更新 index/log

**详细内容** → 参见 `references/ingest.md`

---

### 2. Query — 查询知识

**触发词：** 提问

**概要步骤：** 理解问题 → 读 index 定位 → 遍历 wikilinks 综合回答 → 富维基触发

**详细内容** → 参见 `references/query.md`

---

### 3. Lint — 维基体检

**触发词：** 「维基体检」「lint」「检查维基」

**概要步骤：** 检查断链 → 发现矛盾 → 识别知识空白 → 输出报告

**详细内容** → 参见 `references/lint.md`

---

## 富维基机制

**触发词：** 「存到维基」「这个值得记」「反馈到 wiki」

**判断标准：** 这个回答是否解决了一个以后还会问的问题？

**执行：** 写入 raw → 执行完整 Ingest 流程 → 更新 index 和 log

**详细内容** → 参见 `references/query.md`

---

## Schema 规范来源

项目运行时参照 `~/wiki/CLAUDE.md`。初始化新项目时，使用 `references/schema-template.md` 生成。

---

## 禁止编造规则

**绝对禁止的行为：**
1. **不得虚构 raw 原文中不存在的信息** - 所有内容必须能从 raw 中找到依据
2. **不得基于外部知识填充内容** - 即使你知道相关信息，如果 raw 中没提到，也不能写进 wiki
3. **不得自行补充缺失信息** - 遇到信息缺失时，必须停止并询问用户，而非猜测或编造
4. **不得更改原文含义** - 提取时要忠实于原文，不得曲解或过度解读

**遇到问题时的正确处理：**
```
情况：raw 文件内容异常/缺失/不清晰
❌ 错误：基于已有知识自行填充内容
✅ 正确：向用户汇报问题，询问如何处理

情况：需要提炼概念但信息不足
❌ 错误：从单篇 raw 强行创建概念页
✅ 正确：等待积累更多相关 raw 后再提炼
```

**质量承诺：**
- 所有 wiki 页面内容均可追溯到具体 raw 文件
- 每一条数据、每一个观点都有原文支撑
- 不确定的内容标注 `confidence: low` 并说明原因

---
