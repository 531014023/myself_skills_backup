---
description: Ingest 操作完整步骤、示例和可视化。
---

## Ingest 完整步骤

**触发词：** 提供链接/文件/粘贴内容/文件夹

**输入类型及处理方式：**

| 输入类型 | 处理方式 |
|----------|----------|
| URL | WebFetch/smart-web-fetch 抓取 |
| 粘贴文本 | 直接解析 |
| 文件路径 | 读取并解析 |
| 文件夹路径 | 扫描并批量处理 |

**执行步骤：**

**Step 1. 检查是否已存在（必须）**
- 抓取前，先在 `raw/` 目录搜索该 URL（检查 frontmatter 的 `source` 字段）
- **搜索方法**（Windows PowerShell）：
  ```powershell
  cd ~/wiki/raw && Select-String -Pattern "source: https://..." -SimpleMatch
  ```
- 如果已存在：
  - 向用户报告：`该文章已保存于 raw/YYYY-MM-DD-标题.md`
  - 询问用户：是跳过 / 重新抓取 / 更新现有文件？
- **如果不存在：继续执行 Step 2**

**Step 2. 识别输入类型**
- URL → WebFetch/smart-web-fetch 抓取
- 文本 → 直接解析
- 文件夹 → 扫描所有 .md/.txt/.pdf 等文件（批量处理时每篇单独检查）

**Step 3. 解析内容**
- 提取标题、主要内容、关键术语
- 记录来源信息（URL、文件名、日期）
- 文件名格式: `YYYY-MM-DD-中文标题.md`
- 文件名中的中文标题必须和提取到的标题一致，不能改动

**Step 4. 存储到 raw/**
- 文件名: `YYYY-MM-DD-中文标题.md`（中文标题来自内容，保留所有标点）
- 添加 YAML frontmatter (id, source, date, tags, accessed)
- **id 必须与文件名完全一致（去掉 .md）**

**Step 5. 创建摘要层（必须）**
- **为每篇 raw 创建一篇摘要页**（`摘要/原文标题.md`）
- 摘要是整篇文章的浓缩，不是简单复制
- 包含核心观点、关键数据、重要结论
- frontmatter 的 `raw:` 指向该 raw 文件
- **关键原则：每篇 raw 必须先有摘要页**

### 用户粘贴内容时的处理方式

| 类型 | 处理方式 |
|------|---------|
| **所有内容** | ✅ 必须先存 raw → 创建摘要 → 提取 |

**原则**：无论外部文章还是用户原创内容，都必须先存 raw，保持规范一致性。

**Step 6. 自动提取知识层（必须）**
- 创建摘要后，**立即全面分析**该摘要内容
- **提取所有符合类型的内容**，不设"重要性"门槛：
  - **概念**：文中定义/提到的所有概念（如"坑口价"、"卖put"）
  - **实体**：文中提到的所有公司、人物、品牌（如"陕西煤业"、"段永平"）
  - **对比**：文中的对比分析（如"买put vs 卖put"）
  - **概览**：文中的体系/方法全面介绍
  - **综合**：跨领域思考或个人总结
- 检查是否已存在：
  - 若已存在 → 更新该页面（添加新 raw 关联）
  - 若不存在 → 创建新页面
- **原则**：单篇摘要即可提取，不等待多篇重复

**Step 7. 报告完成**
- 列出本次创建的所有摘要页
- 列出本次创建/更新的所有概念/实体/对比/概览页
- 标注各页面的 `raw:` 关联关系

**Step 8. 更新 index 和 log（必须）**

```
→ 更新 index.md...
✓ index.md 已更新（新增条目索引）
→ 更新 log.md...
✓ log.md 已记录（YYYY-MM-DD Ingest 完成：X个raw、X个摘要、X个概念、X个实体...）
```

**index.md 更新规则：**
- 每创建/更新一个 wiki 条目，同步更新 `index.md` 的相关分类索引
- 按类型分类记录：`摘要/`、`实体/`、`概念/`、`对比/`、`概览/`、`综合/`
- 格式：`[[标题]]`（不带日期）

**log.md 更新规则：**
- 每次操作必须追加一条日志
- 格式：
  ```markdown
  ## YYYY-MM-DD

  ### Ingest
  - 数量：X个raw、X个摘要、X个概念、X个实体...
  - 来源：URL/文件夹路径/粘贴
  - 新增条目：[[标题1]]...
  - 更新条目：[[标题2]]...
  ```
- 保持按日期倒序排列

---

## 提取可视化

```
raw/2026-01-01-文章A.md
raw/2026-01-02-文章B.md
raw/2026-01-03-文章C.md
       ↓【第一步：每篇 raw 创建一篇摘要】
摘要/文章A.md  ← 浓缩文章A的精华
摘要/文章B.md  ← 浓缩文章B的精华
摘要/文章C.md  ← 浓缩文章C的精华
       ↓【第二步：从摘要中提取知识】
概念/安全边际.md  ← 发现A、B、C都提到"安全边际"（多篇提炼 ✓）
概念/商业模式.md  ← 发现B、C都提到"商业模式"（多篇提炼 ✓）
概念/能力圈.md    ← 只有C提到，但概念重要（单篇提取 ✓）
实体/腾讯.md      ← 发现A、C都提到"腾讯"（多篇汇总 ✓）
实体/江南春.md    ← 只有A提到，但人物重要（单篇提取 ✓）
对比/酱香vs浓香.md ← 发现A、B有对比内容（多篇提炼）
概览/老唐投资体系.md ← 综合多篇的投资体系（多篇综合）
```

**关键检查点：**
- ✅ 每篇 raw 都有对应的摘要页？
- ✅ 是否经过摘要层（而非直接从 raw 拆分）？
- ✅ 概念/实体页：多篇提炼（推荐）或单篇提取（允许）？
- ✅ frontmatter 的 `raw:` 字段完整？

---

## 输出示例

```
用户: https://mp.weixin.qq.com/s/xxxxx
AI:
  → 正在抓取...
  ✓ raw/2026-04-12-至简三板斧.md 已创建
  → 创建摘要页...
  ✓ wiki/摘要/至简三板斧.md 已创建
  检查: 这是单篇 raw，暂不创建概念页
  完成: 1个 raw → 1个摘要

用户: 这是我整理的文章 [粘贴内容]
AI:
  ✓ raw/2026-04-12-文章笔记.md 已创建
  → 创建摘要页...
  ✓ wiki/摘要/文章笔记.md 已创建
  完成: 1个 raw → 1个摘要

用户: D:\Articles\投资\
AI:
  → 扫描 12 个文件...
  ✓ 已摄入 12 个文件到 raw/
  → 第一步：为每篇 raw 创建摘要...
  ✓ wiki/摘要/文章A.md ... wiki/摘要/文章L.md (共12个摘要)
  → 第二步：从多篇摘要中提炼概念...
  发现重复概念: "安全边际" (出现在A、B、C中)
  ✓ wiki/概念/安全边际.md 已创建 (raw: [文章A, 文章B, 文章C])
  发现重复概念: "商业模式" (出现在D、E中)
  ✓ wiki/概念/商业模式.md 已创建 (raw: [文章D, 文章E])
  → 第二步：从多篇摘要中汇总实体...
  发现实体: "腾讯" (出现在A、F中)
  ✓ wiki/实体/腾讯.md 已创建 (raw: [文章A, 文章F])
  完成: 12个 raw → 12个摘要 + 4个概念 + 2个实体
```
